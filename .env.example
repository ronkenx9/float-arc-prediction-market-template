# Copy to .env and fill in your values.
# .env is gitignored — never commit it.

# Deployer wallet private key (0x-prefixed)
PRIVATE_KEY=

# Arc Testnet RPC endpoint (default below works)
ARC_TESTNET_RPC=https://rpc.testnet.arc.network
